# Shared Packages

Reusable libraries that can be consumed by `apps/web` and `apps/api`.

- `ui` – design system components
- `sdk` – typed ElevenLabs + data-source clients
- `analytics` – event schema and lightweight client
- `config` – shared tooling configs (eslint, tsconfig, prettier, tailwind)
- `types` – shared domain and API contracts
