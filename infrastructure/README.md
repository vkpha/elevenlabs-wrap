# Infrastructure

Placeholders for IaC (Terraform/Pulumi), CI/CD pipelines, and secrets templates.

- `terraform/` or `pulumi/` for cloud resources
- `ci/` for pipeline definitions
- `secrets.example` for local env templates
